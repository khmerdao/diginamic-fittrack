name: hello-fittrack
on: [pull_request, workflow_dispatch]   # workflow_dispatch = bouton "Run workflow" pour la démo live
jobs:
  build:
    runs-on: ubuntu-latest
    steps: 
      - name: build
        run: echo "build OK"
         

  test:
    needs: build
    runs-on: ubuntu-latest
    steps: 
      - name: test
        run: echo "test OK"
         
